describe '<%= module_name %> service: <%= object_name %>', ->
  beforeEach module '<%= module_name %>'

  beforeEach inject ($injector) ->
    @service = $injector.get('<%= object_name %>')

  it 'should be defined', ->
    expect(@service).toBeDefined()
