module = angular.module '<%= module_name %>'

# Constant
# module.constant '<%= object_name %>', null # value

# Decorator
# module.decorator '<%= object_name %>', ($delegate) ->
#   ->

# Factory
# module.factory '<%= object_name %>', -> # $getFn

# Service
# module.service '<%= object_name %>', constructor
